import React from 'react'
import { useSelector } from 'react-redux'

const CartContainer = () => {
    const {amount,total,cartItems}=useSelector((store))
  return (
      <>
          
      </>
  )
}

export default CartContainer